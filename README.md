# csp1-vue

Vue 3 + Vite conversion of Capstone1 (static portfolio).

## Setup

```bash
npm install
npm run dev
```

## Web3Forms (Contact form)

1. Create a Web3Forms access key.
2. Copy `.env.example` to `.env`
3. Put your key in `VITE_WEB3FORMS_ACCESS_KEY`
